# Deferred Work

## Deferred from: code review of 1-3-shared-wire-contract (2026-06-19)

- **`Card.rank` and Intent numeric/string fields unconstrained at the type level** [shared/src/types.ts] — `rank: number` (not `1..13`), `hostSetLives.lives: number` (not MIN/MAX_LIVES), tokens/`code` unbounded. By design: the contract is type-only (no zod/valibot in the stack); range/integer/token/alphabet validation is owned by Epic 2 `server/src/rules/validate.ts`. Risk if forgotten there: out-of-range client input reaches state silently. Every numeric/string Intent field is a validation obligation for Epic 2.
- **`ProjectedTableState` privacy + self-consistency not type-enforced** [shared/src/types.ts] — `players[].hand?` permits an opponent's card on the wire; `you` vs the matching `players[]` entry are two sources of truth for the local player with no type linkage. Enforced instead by `projectStateFor` + the SM-6 negative-assertion test, both **Story 1.4**. Clarify in 1.4: in `players[]`, omit `hand` for ALL seats (incl. self) until `revealed`; self's card lives only in `you.hand`.
- **`ErrorReason` has no `internal`/`not-implemented` code; non-IntentError throws have no wire representation** [shared/src/types.ts; server/src/project-state.ts] — `projectStateFor` currently throws a raw `Error` (unreachable until 1.4). When real paths exist, Epic 2's dispatch catch must distinguish `instanceof IntentError` (→ `error` event) from unexpected throws (no `ErrorReason` fits — log + generic close). Decide whether to add an `internal` code to the union at that point.

## Deferred from: code review of 1-1-pre-build-spike (2026-06-19)

> Source story is a THROWAWAY spike. None of these require touching the (about-to-be-deleted) spike code; they are carried as known limitations / downstream follow-ups. The GO decision stands.

- **AC2 false-PASS verdict strings** [spike/src/table-server.ts:159-166,172-197] — `/inspect` & `/reload-coerce` emit "PASS"/`coerced:true` purely from `round===null`, which holds on any warm/cold/fresh instance, so the string does not prove a forced restart caused it. The findings doc already records the cold-restart half as an optional user step; no durable artifact is wrong. Risk if not noted: a future reader trusts the verbatim PASS over the findings caveats.
- **AC3 `getWebSockets()` returns 0 under partyserver default `hibernate:false`** [spike/src/table-server.ts:202-215,225-234] — this IS the recorded AC3 defect. The `alarm()` preserve-branch (sockets>0) is dead code in the spike. **Correction owned by Story 1.11:** enable partyserver Hibernation (preferred — also delivers the idle-billing benefit the $0 gate/SM-7 assumes) or count `getConnections()`; verify partyserver hibernation accepts via `ctx.acceptWebSocket()`.
- **AC3 `alarm()` GC decision never fired/observed** [spike/src/table-server.ts:225-234] — the 60s idle alarm was never awaited in the deployed run; the GC self-delete/preserve behavior was only probed synchronously via the HTTP endpoint. Folds into the Story 1.11 GC correction (verify the alarm path empirically there).
- **AC1 concurrent-claim race not exercised** [spike/src/table-server.ts:84-113] — only the sequential happy path was proven; the verdict "Claim-on-create CONFIRMED" reads stronger than observed. **Follow-up owned by Story 1.6:** ensure the claimed-marker read+write is atomic within the DO's single-threaded turn (no two creators both seeing "unclaimed").

## Deferred from: code review of 1-2-ac-driven-project-initialization (2026-06-19)

- **Client `tsc -b` permanently "out of date"** [client/tsconfig.json] — `noEmit:true` + `composite:false` means `tsc -b` checks for an output file that can never exist, so it rebuilds from scratch every run (incremental cache never registers up-to-date). Functionally fine — it does typecheck the client `.ts` files. Cosmetic/perf only; revisit (`tsc -p` or `tsc --noEmit` for the client leaf) if client typecheck time becomes a problem.

## Deferred from: code review of 1-2-ac-driven-project-initialization (2026-06-19, round 2)

- **partyserver kebab-case routing untested** [server/src/index.ts] — partyserver lowercases the binding name, so the URL namespace is `/parties/table/<name>`, NOT `/parties/Table/...`. This is documented in a comment only; `scaffold.do.test.ts` uses `env.Table` directly and bypasses routing, so nothing asserts a real request routes correctly. A client hitting the wrong-cased path 404s with the gate fully green. Add a routing assertion in the Story 1.7 (multi-device join) integration tests against `wrangler dev`.
- **`rules/**` purity does not ban scheduling/ambient non-determinism** [eslint.config.js] — the denylist is a fixed enumerated set; `setTimeout`, `setInterval`, `queueMicrotask`, `Intl` (timezone/locale → non-deterministic), `WeakRef`, `FinalizationRegistry` (GC-observable) are not banned. Extend the denylist when the pure engine (Epic 2) is actually authored and the real surface is known.
- **Version-pin / compatibility_date freshness is comment-only** [server/wrangler.jsonc] — "re-verify recent enough at run time" relies on a human reading the comment at deploy; no automated assertion that the installed wrangler runtime supports the pinned `compatibility_date` (SQLite-in-DO + WS hibernation). Re-verify at first real deploy.
- **No `engines` field** [package.json] — the Node floor for vite 8 / vitest 4 / wrangler 4.103 is unenforced locally; CI pins Node 22 but a contributor on an older Node hits a divergent failure mode. Add `"engines": { "node": ">=22" }` when convenient.
- **`.ts`-as-package-entry depends on bundler resolution** [shared/package.json] — `main`/`types`/`exports` point at `./src/index.ts`; only safe under vite/esbuild/wrangler/Bundler resolution. A node-native consumer would fail to load `.ts`. Acceptable for the internal monorepo; only a problem if anything ever resolves these packages node-natively.
- **Root `npm test` covers only the `server` workspace** [package.json] — `client`/`shared` have no test gate, though CI presents `npm test` as the test step. Deliberate for now (client/shared are stubs per the 1.3 scope boundary; nothing meaningful to test). Broaden the root `test` script to fan out across workspaces once client/shared have real logic (Story 1.3+).
- **`.send`/`.broadcast` egress ban is `**/*.ts`-scoped (server-only)** [eslint.config.js] — `.svelte`/`.js` are unguarded and `eslint .` doesn't parse `.svelte`. This is correct by design: SM-6 governs the SERVER's single send site (`push-state.ts`); the client legitimately calls `socket.send(intent)`. Low-priority follow-up: add a comment to the gate documenting that the scope is intentionally server-only, to prevent a future reader mistaking it for an incomplete ban.
- **Vitest test-file partition is convention-only** [server/vitest.config.ts] — the two projects exhaustively cover `*.test.ts` (node) and `*.do.test.ts` (Workers), but a file named `*.spec.ts` / `*.workers.ts` matches neither and runs in NO project (green CI, zero coverage). Documented via an in-file naming-convention comment this round. If the team wants a hard guard, add a meta-test that globs `src/**/*.ts`, flags any file containing `test(`/`it(`/`describe(` that doesn't end in a recognized suffix, and fails. Deferred — convention comment is sufficient for now.

## Standing obligations from: Story 1.4 (SM-6 inference channels)

> SM-6 is "no card SEEN **or INFERRED**." Story 1.4 closed the SEEN channel mechanically (`projectStateFor` + the standing negative-assertion test in `server/src/project-state.test.ts`) and pinned inference channel (a) constant message shape. The two remaining inference channels are NOT code in 1.4 — there is no turn/peek/swap code yet — so they are carried here as STANDING review obligations, each tagged with its owning Epic 2 turn story. (On-disk mapping: peek = Story 2.5, draw = Story 2.6; the epics' Mary-review parenthetical's "2.7" is a typo — Epic 2 has no Story 2.7.)

- **(b) Timing-indistinguishability of turn responses** [Story 2.4 (swap/keep), Story 2.5 (peek), Story 2.6 (last-player swap / drawFromDeck)] — a turn-scoped response (`swap` / `keep` / `peek` / `drawFromDeck`) MUST be timing-indistinguishable by card value: NO value-dependent server-latency branch. A handler that does more work (or less) depending on the hidden card rank would leak the value through response timing even with the value absent from the payload. Each of these stories must keep its handler's work-per-intent independent of any hidden card value.
- **(c) No behavioral tell on any surface** [Story 2.4 (swap/keep), Story 2.5 (peek), Story 2.6 (last-player swap / drawFromDeck)] — no rendered surface may expose a behavioral tell (a glow, an enabled/disabled state, a count, or any other value-dependent UI affordance) that lets a neighbor infer a hidden card. The value-free server-computed fields (`isLastPlayer`, `isAlive`, etc.) exist precisely so the client never derives turn/elimination/value logic locally; these stories must not introduce a client-side affordance keyed on a hidden card value.

## Deferred from: code review of story-1.4 (2026-06-19)

- **Reveal-true assigns `entry.hand = undefined` for a seat with no hand entry** [server/src/project-state.ts:56 → Story 3.2] — when `revealed === true` and `round.hands[p.id]` is missing, `if (revealed && round) entry.hand = round.hands[p.id]` makes the `hand` key PRESENT with value `undefined` (breaches the omit-when-absent / constant-shape invariant pre-serialization; `JSON.stringify` currently masks it on the wire). Not a 1.4 defect — the reveal-true path is Story 3.2 / Epic 3, and AC3 forbids forward-binding to it. When Story 3.2 implements the reveal-true behavior + its privacy test, guard the assignment: `const h = round.hands[p.id]; if (h) entry.hand = h;`.
- **Reveal-true projection path has zero test coverage** [server/src/project-state.test.ts → Story 3.2] — the only branch that exposes other players' cards (`revealed === true`) is untested. This is by design (AC3 scopes the standing test to `revealed === false`; the reveal-true privacy assertion is Story 3.2's acceptance). Story 3.2's test should assert every `players[].hand` is present and equals `round.hands[id]` at reveal — and should also catch the `entry.hand = undefined` item above.
- **Branch-coverage gaps in the SM-6 test** [server/src/project-state.test.ts] — the body handles these via `round?.…`, but the test exercises only the happy path (3-seat round, `revealed === false`, projecting for host A at seat 0). Untested branches: `round === null` (lobby / D2.1 reload coercion), a non-host projector, a projector not at seat 0, a `playerId` not seated (spectator fallback → `isAlive`/`isConnected` false, no `you.hand`), and degenerate rosters (empty / single / duplicate-id). Optional hardening for a future test-strengthening pass; acceptable to leave for this story's scope.

## Deferred from: code review of story-1.5 (2026-06-19)

- **`createSocket` silently connects to a malformed URL when `VITE_WS_URL` is unset** [client/src/socket.ts:62-66 → Story 1.6/1.7] — `host: import.meta.env.VITE_WS_URL ?? ""` yields `host: ""`. Verified against `node_modules/partysocket/dist/index.js`: an empty host does NOT throw at construction (the empty-host guard lives only in `reconnect()`); `getPartyInfo` builds `wss:///parties/main/<room>` (empty authority) and `_connect()` fires immediately, so with `maxRetries: 0` the single failed connect is swallowed — a misconfigured build gets a dead socket with no diagnostic. Deferred: the Story 1.5 spec explicitly sanctions the empty fallback for this issuance-only seam ("fall back is fine for the seam"), and `socket.ts` is not mounted yet — no live connection can fail until the connection flow (Story 1.6 `createRoom` / 1.7 `joinRoom`) actually constructs the socket. That flow owns fail-loud config behavior: when it mounts `createSocket`, assert `VITE_WS_URL` is present (throw / surfaced error) instead of tolerating `host: ""`.

## Deferred from: code review of story-1.6 (2026-06-19)

- **`reconcileSummaryToState` bumps `phaseToken+1` on D2.1 coercion but never re-persists** [server/src/persistence.ts:434-446 → Story 2.2/2.3] — on DO wake from a lost live-round, `onStart` rebuilds state with `phaseToken+1` in memory but does NOT call `persistSummary`, so the durable "table" key keeps the un-bumped token. If the DO is evicted again before any handler persists, the next wake reloads the OLD token and coerces again — phaseToken monotonicity across repeated restarts is not guaranteed. Out of 1.6 scope: coercion (`LIVE_ROUND_PHASES`) can only fire mid-round, which arrives with the phase machine (Epic 2 / Stories 2.2-2.3). That work exercises and owns this seam; whoever implements the live-round restart path should re-persist the coerced state in `onStart` (or on first projection) so the durable token advances.

- **`createRoom` name CONTENT validation (empty/whitespace/length)** [server/src/handlers.ts + lobby UI Stories 1.9a/1.10] — Story 1.6's review added a SHAPE guard (reject missing payload / non-string `name` with `IntentError`), but does NOT validate name content: an empty string, whitespace-only, or an oversized name is accepted verbatim into `players[0].name` and projected to clients. Deferred per Decision #1 ("lightweight phase-checking only, no validation lib" in lobby) — name-content rules are presentation/UX validation that belongs to the lobby UI story where the name input is actually wired. When that story lands, decide max length + trim/empty handling (client-side input constraint and/or a server-side length clamp) so a degenerate name can't reach the roster.

## Deferred from: code review of story-1.7 (2026-06-19)

- **Resolved join socket returned with no transport listeners** [client/src/socket.ts:75 → lobby UI Stories 1.9a/1.10] — `joinRoomAndListen` removes the `close`/`error` listeners in `cleanup()` before resolving with the kept-open socket; with `maxRetries:0` a post-join WS drop is silent (the lobby goes dead with no diagnostic). The helper is not mounted yet, so this is a forward seam: when the lobby UI consumes the resolved socket, attach a liveness/close handler (or a UI "disconnected" banner) so a dropped lobby socket is observable.

- **`handleJoinRoom` pushes to the in-memory roster before `await persistSummary`** [server/src/handlers.ts:347 → pattern, pre-existing] — mirrors `handleCreateRoom`'s push-before-persist (handlers.ts:113). If `persistSummary` rejects, the in-memory cache keeps the appended seat while the durable summary does not; the fan-out never runs (dispatch rethrows the non-IntentError before fan-out, and the client hangs to its timeout with a misleading `timeout` reason). No other device sees a ghost, but the next successful join computes `seatIndex = players.length` off the inflated count. Bounded by DO storage reliability; revisit if persistence becomes a failure surface.

- **Name content (empty/whitespace/duplicate/oversized) accepted on the live join wire** [server/src/handlers.ts:300 → Stories 1.9a/1.10, deferred-work #54] — same class as the deferred createRoom name-content item; the shape guard only checks `typeof name === "string"`. A non-UI client can seat a blank/oversized/duplicate name now via the live `joinRoom` path. Owned by the lobby-UI validation story.

- **`seatIndex = players.length` collides once any path removes a player** [server/src/handlers.ts:343 → Epic 4 hostRemovePlayer] — not reachable in 1.7 (no removal; `markDisconnected` only flips `isConnected` and retains the seat). Epic 4's `hostRemovePlayer` will splice the roster array; at that point the next join reuses a `seatIndex` already held by an existing player. Switch to a monotonic seat counter (or `max(seatIndex)+1`) when removal lands.

- **AC-1.7.4 activation gate verified only by a manual, out-of-CI `.mjs` script** [server/test/integration/multi-device-join.mjs → optional CI smoke job] — compliant as specified (the vitest-pool-workers `do` project cannot drive real concurrent WS upgrades, so the spec mandates a `wrangler dev` integration test, spec line 37). It is wired as `npm run test:integration` but excluded from `npm test`; its green status rests on the dev's recorded run, not a re-runnable gate. Consider a CI job that boots `wrangler dev` and runs the harness so the activation gate is regression-protected.

## Deferred from: code review of story-1.8 (2026-06-19)

- **`sendIntent` does not guard socket `readyState` and has no send-failure path** [client/src/socket.ts → Stories 1.9a/1.10] — `sendIntent(socket, intent)` is `socket.send(JSON.stringify(intent))` with no liveness check. With `maxRetries:0`, calling it on a CONNECTING/CLOSING/CLOSED socket either throws `InvalidStateError` or silently drops the frame, so a Host's set-lives could be lost with no client signal. Deferred per the Story 1.8 scope decision (Task 4): this is the builder-only send seam — `createRoomWithRetry`/`joinRoomAndListen` resolve and detach their own listeners, and the Host Lobby surface (1.9a/1.10) owns the kept-open socket's lifecycle + liveness. That surface should check `readyState`/surface a disconnected state when it mounts the stepper onto the live socket. Same class as the deferred 1.7 "resolved join socket returned with no transport listeners" item.

- **`handleHostSetLives` mutates in-memory state before `await persistSummary` (not atomic on a write failure)** [server/src/handlers.ts:292-294 → pattern, pre-existing] — sets `startingLives` + every `players[].lives` in memory, then `await persistSummary`. If the persist rejects, the live `host.table` holds the new values while the durable summary does not, and dispatch rethrows before the fan-out (client hangs to its timeout). Mirrors the identical mutate-then-persist ordering in `handleCreateRoom`:113-114 and `handleJoinRoom`:216 — a codebase-wide convention, not introduced by 1.8. Bounded by DO storage reliability; revisit only if `ctx.storage` becomes a real failure surface (snapshot-and-rollback, or persist-then-mutate).

- **AC-1.8.2 "no fan-out to other connections" is not positively asserted with a second open socket** [server/src/table-server.do.test.ts → test hardening] — the `NHST` test confirms the guest gets `not-host` and the persisted summary is unchanged, but it does not hold a separate host socket open to assert the host received NO `tableState` push on the refusal. The code path is correct (the `not-host` throw precedes `fanOut`), so this is a coverage gap, not a behavior bug. A future hardening pass could keep the host socket open and assert it sees no event after the refused guest send.

## Deferred from: code review of story-1.9a (2026-06-19)

- **Empty/undefined `winnerIds` at `gameOver` routes the real winner to `eliminated`** [client/src/route-from-state.ts:48 → Epic 3] — `state.winnerIds?.includes(you.playerId)` is falsy when `winnerIds` is `[]` or absent, so at `gameOver` EVERY player — including the actual winner — routes to `eliminated`; no one ever sees Winner. The projector does not populate `winnerIds` this story (`server/src/project-state.ts` leaves it unset; Epic 3 / Story 2.4 beats), so the path is not reachable in 1.9a, but undefined `winnerIds` is the DEFAULT projection shape today. When Epic 3 produces a real `gameOver`, ensure `winnerIds` is populated (and/or add a sole-survivor fallback in the router) before this branch goes live.

- **Surface stubs drop co-winners and silently degrade on an unknown id** [client/src/surfaces/Winner.svelte:7, client/src/surfaces/Waiting.svelte:8 → Story 1.10 real content] — `Winner` uses `players.find(p => winnerIds?.includes(p.id))?.name` and renders only the FIRST match, so a tie / multi-winner game shows just one name; `Winner`/`Waiting` fall back to generic copy ("Winner!" / "Hang tight.") when `winnerIds`/`currentTurnId` reference an id not present in `players[]` (disconnected/pruned seat). Harmless at stub fidelity; the 1.10 real-content pass should render all winners and decide the unknown-id behavior deliberately.

- **`revealed` short-circuits the router to `showdown` for ANY phase, with no phase gate** [client/src/route-from-state.ts:53 → Epic 3] — `if (phase === "showdown" || state.revealed) return "showdown";` is evaluated before the `roundResult` and `!isAlive` branches, and `revealed` is projected straight from `round.revealed` (server/src/project-state.ts:21). If the server ever reaches `phase === "roundResult"` while `round` (and thus `revealed: true`) has not been cleared, every device — alive or eliminated — routes to `showdown` instead of `roundResult`/`eliminated`. The "showdown wins over eliminated" precedence is the spec-recommended Decision #6 choice and is correct as a precedence; the open question is whether to GATE `revealed` on phase. Deferred because the router runs only on injected fixtures in 1.9a (no live projector exists yet) and the spec explicitly flags this call for Epic 3 to confirm against the real Showdown/Eliminated resolution code. Epic 3 action: confirm the projector always clears `round` (→ `revealed:false`) on the `showdown→roundResult` transition; if it cannot guarantee that, gate the reveal branch on `phase === "showdown" || phase === "allActed"` and add a `roundResult + revealed:true` router test.

## Deferred from: code review of story-1.9b (2026-06-19)

- **Button `locked` is a plain `let`, not `$state`** [client/src/components/Button.svelte:45 → Story 1.10] — correct today (the flag is read synchronously inside `handleClick` and never appears in the template, so the debounce works without reactivity), but it silently cannot drive UI: a future dev who writes `disabled={locked}` to add a debounce-pending affordance will get no re-render. When 1.10 wants a visible pending/disabled-during-debounce state, promote `locked` to `$state`.
- **Button debounce `setTimeout` is never cleared on unmount** [client/src/components/Button.svelte:50 → Story 1.10] — harmless now (the callback flips a non-reactive plain `let` on an already-dead closure, so no "set state after unmount"), but if a surface unmounts within `DEBOUNCE_MS` (350ms) of a tap — plausible for Deal/Swap/Keep which transition immediately — the timer still fires. This becomes a real defect the moment `locked` is promoted to `$state` (see item above). Fix together: capture the timer id and `clearTimeout` it in `onDestroy`.

- **`Eliminated.svelte` couples the surface to the `" — "` em-dash separator in `ELIMINATED`** [client/src/surfaces/Eliminated.svelte:46 → Story 3.5] — `const [lead, tail] = ELIMINATED.split(" — ")` then `<h1>{lead}.</h1><p>{tail}</p>` is fragile: a copy reword with a hyphen/en-dash makes `tail` `undefined` (empty `<p>`, sentence collapses into the `<h1>`); a second `" — "` drops a clause; the synthesized `.` after `{lead}` assumes the lead never carries terminal punctuation. AC-1.9b.3 explicitly permits the two-line visual split, so it's within spec and correct today. **Reason for deferral:** Story 3.5 rebuilds the Eliminated surface with full layout context — harden there (or restructure copy.ts to carry the split) rather than patching a stub now.

## Deferred from: code review of story-1.10 (2026-06-19)

- **`adoptSocket` leaks its message listener and double-subscribes on re-adopt** [client/src/lib/table-store.svelte.ts:60-64 → reconnect flow / AR-12] — `adoptSocket` adds an inline-arrow `message` listener (no reference kept to remove it) and unconditionally reassigns `liveSocket` without closing the prior socket. A second create/join in one session would leave two live sockets both feeding `handleSocketMessage`, flipping the store between two tables' projections. Not reachable in 1.10 (reconnect disabled, `maxRetries:0`, one create/join per session); fix when the reconnect/AR-12 flow lands — keep a listener ref + `removeEventListener`/`close()` the old socket (or early-return if already adopted).
- **Malformed `tableState` payload would crash the live router** [client/src/lib/table-store.svelte.ts:51-53 → hardening] — `handleSocketMessage` writes `event.payload as ProjectedTableState` on any `{type:"tableState"}` with no shape check. A partial-but-valid-JSON payload (e.g. `{}`) bypasses the `state === null` guard in `routeFromState` and throws on `state.phase`/`you.isAlive` on the live surface. The store deliberately chose tolerant parsing and the server is the trusted projection authority, so this needs a malformed/buggy server frame to trigger. Harden with a minimal shape guard (e.g. require `phase` + `you`) before adopting, if/when untrusted frames become possible.
- **Dropped frame in the create/join → adopt listener handoff gap** [client/src/lib/table-store.svelte.ts:60-64 → reconnect/sequencing design] — the create/join helper detaches its one-shot listener, then `adoptSocket` attaches the persistent one. A projection arriving in that window has no listener and is lost (no sequence/replay to recover it). Per-connection WebSocket ordering makes this narrow today; address with a sequence/`phaseToken` monotonicity guard when reconnection/replay is designed.
- **No Enter-to-submit on the code/name form** [client/src/surfaces/Home.svelte:98 → UX enhancement] — the `<form>` prevents default submit and no Button is wired to form submit, so filling all 4 code slots + name and pressing Enter does nothing (no keyboard path to Start/Join). No AC requires it; add an `onsubmit` → `doJoin`/`doStart` dispatch when polishing keyboard UX.
- **Lives stepper has no optimistic feedback; rapid taps unreliable** [client/src/surfaces/Lobby.svelte setLives → reconnect/feedback polish] — the displayed value follows the server echo (`state.startingLives`), so two quick `+` taps before the echo lands both compute `current+1` and the no-op guard swallows the second; on a slow/failed socket a tap shows zero visible change. **Reason for deferral:** server-authoritative display is the spec intent (Decision #5); rapid-tap is an edge case best revisited alongside the reconnect/feedback polish rather than introducing an optimistic local value now.

## Deferred from: code review of 1-11-room-garbage-collection-zero-cost-backstop (2026-06-20)

- **`onClose` never shortens the alarm — a cleanly-emptied room holds its Room Code for up to ~3h** [server/src/table-server.ts onClose] — activity arms the idle alarm (onConnect, post-dispatch) but the last clean departure does nothing, so a room that empties cleanly is reclaimed only when the idle TTL eventually fires (up to 3h, plus debounce slop). Idle GB-s still stop accruing once the socket hibernates/closes; this is purely a code-reclaim-latency gap. **Reason for deferral (Decision B):** the 3h backstop still bounds it and prompt code-reuse pressure is low for a party game. Revisit with an `onClose` override that, when it drops the last connection, shortens the alarm (or fires GC) — if/when Room-Code churn matters.
- **Ghost/never-closing socket defeats the 3h backstop** [server/src/table-server.ts onAlarm preserve branch] — the active branch re-arms `now + IDLE_TTL_MS` on every alarm fire, so a hibernated socket whose TCP never cleanly closes (the architecture-D7 backstop case the story exists to cover) keeps resetting the TTL from `now`; the room is only reclaimed once the CF runtime evicts the ghost from `ctx.getWebSockets()`. The TTL is therefore "3h after the last alarm fire while a socket lingers," not "3h after the last real activity." Inherent to Cloudflare's hibernated-socket eviction semantics, not introduced incorrectly by this change — carry as a known limitation of the "$0 backstop." Revisit if reclaim latency on phantom sockets proves to matter (e.g. cap consecutive re-arms, or cross-check socket age).
