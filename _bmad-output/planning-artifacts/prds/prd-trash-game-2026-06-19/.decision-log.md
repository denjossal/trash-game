# Decision Log — Trash PRD

Canonical memory and audit trail for the Trash PRD run. Every decision, change, and override is recorded here as the conversation unfolds.

Created: 2026-06-19

---

## Context carried in

- **Intent:** Create (no prior PRD; clean workspace).
- **Upstream input:** Product brief at `_bmad-output/planning-artifacts/briefs/brief-trash-game-2026-06-19/brief.md` (coached, decision-logged). Extracted via subagent at start of run.
- **Stakes:** Hobby / personal — "made for the maker's own people." Length target ~2 pages, scale to FRs/concerns as needed.

## Decisions

- **Working mode:** Fast path. Batch open game-rule questions, then draft full PRD with `[ASSUMPTION]` tags. (2026-06-19)

### Game-rule decisions (close brief's open questions)

- **Starting lives:** Host chooses 1-5 at table setup. Tunes session length to the group. (Closes brief OQ "starting lives.")
- **Deck threshold:** Auto — app uses 1 deck up to ~10 players, 2 decks above ~10. Hidden from Host; zero thought. Exact threshold = 10 `[ASSUMPTION]`, tunable. (Closes brief OQ "1-vs-2-deck threshold.")
- **Player range (MVP):** 2-20. Includes heads-up; requires 2-player rule + large-table showdown UI. (Closes brief OQ "min/max players.")
- **Turn order:** Host starts the first round; play passes to each player's right. "To your right" everywhere (per brief Decision 11). (Closes brief OQ "starting player + rotation.") Starting player for subsequent re-deals = Host again `[ASSUMPTION]` unless told otherwise.
- **Heads-up (2-player):** P1 chooses Swap (force onto P2) or Keep; P2 is last player and may Swap with P1 OR draw a random card from the deck. "All about luck" on the deck draw. (Closes brief OQ "heads-up tweak.")
- **Showdown resolve:** App computes lowest, highlights loser(s), deducts life automatically. Essential at ~20 cards. (Matches brief large-table note.)
- **King handling:** App ENFORCES the block (reverses brief Decision: brief said "pure social, app does nothing"). User chose enforcement. CRITICAL privacy reconciliation: block is enforced server-side; swapper is told only "that player can't be swapped into" — the King card value is NEVER leaked to the client. Preserves the one hard integrity rule. To be written into the relevant FR with the privacy interaction flagged explicitly.

### Carried from brief (not re-litigated)

- Positioning, two core beats (squirm + reveal), two design gates (Host + reluctant player), $0 cost, server-authoritative + private secret cards (hard rule), comedy FX deferred to v1.1 (MVP keeps minimal reveal), Powers mode = v2, no reconnection in MVP (but stable playerId from day one), pure transport-agnostic rule functions, Fisher-Yates + CSPRNG shuffle, TypeScript, PartyServer/Cloudflare direction (tech detail → addendum).

### Finalize — reviewer findings & resolutions (2026-06-19)

Reviewers run: rubric quality walker, adversarial privacy/rules red-team, brief→PRD reconciliation. Full outputs in `review-rubric.md`, `review-adversarial.md`, `reconcile-brief.md`.

- **F-1 (CRITICAL) King Block leaks.** Server-enforced King Block deterministically leaks "neighbor holds a King" (only block condition → biconditional inference), breaking SM-6/§11.1. Also leaks to third parties via turn indicator (F-6). **RESOLVED: revert to social-only King handling** (matches brief's original stance). App does nothing for Kings; King is just highest card at Showdown. User chose this. Dissolves F-1, F-6, and the FR-8 privacy contradiction. **This re-reverses the earlier King-enforcement decision** — net effect: PRD now matches the brief on Kings.
- **F-2 (CRITICAL) Swap undefined/contradictory.** Glossary garbled; FR-6 read as one-way push while FR-7 said "swap with" (reciprocal); one-way push breaks one-card-per-player invariant. **RESOLVED: Swap = EXCHANGE** (you give your card to the right AND receive theirs; each player always holds exactly one card). Matches Screw-Your-Neighbor source game. User chose this. The "shove my trash away" feeling is preserved framing; mechanically it's an exchange.
- **F-3 (HIGH) Last Player / one-pass.** Defined "Last Player" relative to the *starting Player* (survives future rotation), and stated turn order is exactly one pass (no wrap to starting player). OQ-1 (re-deal starting player) kept open but no longer blocks the Last Player definition.
- **F-4 (HIGH) Two-deck showdown.** Resolved: value-only comparison (suit ignored), two decks form one merged shuffled draw pile, all-tied degenerate case explicit (everyone tied = everyone loses a Life).
- **F-5 (MED) Peek auto-hide.** Added: Card hidden by default, peek requires explicit hold/tap, auto-re-hides, never persistently shown.
- **F-7 (MED) Deck reconstitution.** Added: each Deal reconstitutes and reshuffles the full Deck(s); removed Cards return.
- **F-8 (LOW) Deal private+simultaneous.** Added Consequence: each device gets only its own Card value; all devices transition to dealt state together.
- **F-9 (LOW) Zero-survivors tie.** Added FR-12 Consequence: if the last remaining Players all hit zero Lives in one Showdown, they share the win `[ASSUMPTION]`.
- **Reconciliation weakened items:** problem-reframe honesty added to §1 (deck = enabling insight, not driver); Powers-mode "numbers + Ace stay pure / player-triggered" rationale → addendum; SM-3 narrowing-with-FX flagged. Fidelity otherwise high; King reversal was already acknowledged in-doc.

### Finalize — open-item triage decisions (2026-06-19)

- **OQ-1 (re-deal start) RESOLVED:** Host starts first Round; previous Round's Loser starts each subsequent Round (FR-5, FR-12). Sub-assumptions: multi-Loser tiebreak = earliest-seated tied Loser from prior Starting Player; if Loser-to-start was eliminated, next surviving Player to their right starts. Both tunable.
- **OQ-2 (deck threshold) RESOLVED:** 1 deck for 2-10 Players, 2 decks for 11-20. Now a decision, not an assumption.
- **OQ-4 (host controls) RESOLVED — SCOPE ADDITION:** all three pulled into MVP (change Lives, remove Player, reassign Host). New feature §4.5 / FR-14. Two implementation sub-details left as `[ASSUMPTION]` for UX/build (Lives-change effect; mid-Round removal timing).
- **OQ-5 (late join/leave) RESOLVED:** no join-in-progress; lobby join up to first Deal only; leaver stops taking Turns, table continues/restarts socially (FR-3). Full reconnection stays out of MVP.
- **OQ-6 (room code) RESOLVED:** 4 uppercase letters, ambiguity-safe set (FR-1). Sub-question (Table/Code TTL) left open for build.
- **OQ-3 (two-deck tie frequency)** intentionally left OPEN — genuine playtest question, not a phase-blocker.
- FR count grew to **14** (added FR-14). All `[ASSUMPTION]` tags re-synced to §9 index.

### Finalize — polish & close (2026-06-19)

- **Structural pass:** structure judged sound for hobby stakes; only fix applied = removed a duplicated positioning sentence in §1 Vision.
- **Prose pass:** body clean. Editorial call: User Journeys (§2.3) kept in warm narrative voice (lowercase common nouns) on purpose — the one place the brief's tone breathes; Glossary "verbatim" rule treated as no-synonyms, not force-capitalize-in-prose. Applied capitalization only to multi-word proper terms that read as labels even in narrative (Room Code, Last Player, Re-deal).
- **Status:** `prd.md` frontmatter set to `status: final`, updated 2026-06-19.
- **Artifacts:** `prd.md`, `addendum.md`, `.decision-log.md` retained. Working review/reconcile files (`review-rubric.md`, `review-adversarial.md`, `reconcile-brief.md`) retained for traceability.
- No external handoffs configured (`external_handoffs` empty). No `on_complete` step.
