{
  "name": "@trash/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && vite build",
    "pages:deploy": "wrangler pages deploy dist --project-name=trash-game --branch=main",
    "preview": "vite preview",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "typecheck": "svelte-check --tsconfig ./tsconfig.json && tsc -b"
  },
  "dependencies": {
    "@trash/shared": "0.0.0",
    "partysocket": "1.2.0",
    "svelte": "5.56.3"
  },
  "devDependencies": {
    "@playwright/test": "1.61.0",
    "@sveltejs/vite-plugin-svelte": "7.1.2",
    "@testing-library/svelte": "5.3.1",
    "jsdom": "29.1.1",
    "svelte-check": "4.4.1",
    "vite": "8.0.16",
    "vite-plugin-pwa": "1.3.0"
  }
}
