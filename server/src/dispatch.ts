// dispatch.ts — intent router + phase-legality; the single try/catch that turns an
// IntentError into a targeted `error` event.
// [Source: architecture.md#Canonical-round-trip, #The-rules table — single-error-catch-site]
//
// SCOPE (Story 1.2): seam file only. Implemented with the intent handlers (Story 1.6+).
export {};
