// @trash/shared — THE WIRE CONTRACT, imported by name by both server and client.
//
// SCOPE (Story 1.2): this is a minimal compiling stub so server + client can
// import "@trash/shared" by name and the empty scaffold typechecks.
// The FULL contract (Card, Phase, Intent, ServerEvent, ErrorReason, IntentError,
// ProjectedTableState, TableState, Player, Round) is authored in Story 1.3.
export * from "./types.js";
export * from "./config.js";
