// Wire-contract types. STORY 1.2 STUB ONLY — Story 1.3 authors the full contract here:
// Card, Phase (7 literals), Intent, ServerEvent, ErrorReason, IntentError (pure class),
// ProjectedTableState, TableState, Player, Round.
//
// A single placeholder export keeps this a real module that server + client can import
// by name, proving the @trash/shared workspace wiring works end-to-end.
export type SharedContractVersion = "0.0.0-stub";
