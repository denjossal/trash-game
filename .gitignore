# Throwaway pre-build spike (Story 1.1) — must NOT become product code.
# The real repo is scaffolded fresh in Story 1.2. Only the findings doc is durable.
/spike/

# OS / editor cruft
.DS_Store

# Node
node_modules/

# Build output
dist/
client/dev-dist/
.svelte-kit/

# Wrangler / local env
.wrangler/
.dev.vars

# Vite local env (developer-specific; e.g. VITE_WS_URL for local wrangler dev)
.env.local
.env.*.local

# TS build info
*.tsbuildinfo

# Playwright e2e (Epic 2 retro action 3) — artifacts only; specs + config under client/e2e ARE tracked.
client/test-results/
client/playwright-report/
client/blob-report/
client/.playwright/

# BMad tooling — local dev workflow, not part of the project source.
# (Planning + implementation artifacts under _bmad-output/ ARE tracked.)
/.claude/
/_bmad/
